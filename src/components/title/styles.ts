import { StyleSheet } from 'react-native';
import { genericStyles } from 'src/styles/generic.styles';

export const styles = StyleSheet.create({
  scene: {
    ...genericStyles.horizontalCenter,
  },
  title: {
    marginTop: '20%',
  },
});
